# Calendar ICS URLs (sensitive - keep in .env, not in docker-compose)
# Get Apple calendar URL: iCloud Calendar -> Share Calendar -> Public Calendar -> copy webcal:// URL (change to https://)
# Get Google calendar URL: Calendar Settings -> "Secret address in iCal format"

CALENDAR_CONFIG_FAMILY_URL=https://p123-caldav.icloud.com/published/2/your-calendar-id
# CALENDAR_CONFIG_WORK_URL=https://calendar.google.com/calendar/ical/your-calendar-id/basic.ics
