{
  "name": "calendar",
  "version": "1.0.0",
  "description": "Family calendar display for iPad",
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "start": "node --experimental-strip-types --env-file-if-exists=.env --watch src/server.ts",
    "start:prod": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ical.js": "^2.1.0",
    "pug": "^3.0.3",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.17",
    "typescript": "^5.8.3"
  }
}
