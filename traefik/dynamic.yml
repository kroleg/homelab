http:
  routers:
    claude-chat:
      rule: "Host(`chat.internal`)"
      entrypoints:
        - web
      service: claude-chat

  services:
    claude-chat:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:3005"
