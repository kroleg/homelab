{
  "name": "claude-chat",
  "version": "1.0.0",
  "description": "ChatGPT-like web interface using Claude CLI as backend",
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "start": "node --env-file-if-exists=.env --watch src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.17",
    "typescript": "^5.8.3"
  }
}
